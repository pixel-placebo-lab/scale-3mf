# macOS here uses a case-insensitive APFS volume, so "tests/" and "Tests/"
# resolve to the SAME directory (they cannot coexist). The Python pytest
# suite lives at Tests/test_scale_3mf_pytest.py; a legacy unittest file
# (test_scale_3mf.py) sits at the repo root. Bare `python3 -m pytest -q`
# collects both and is green.
