name: CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    name: Swift build & test (macOS 14)
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: swift build

      - name: Run tests
        run: swift test
